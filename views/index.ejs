<%- include ('partials/header.ejs'); %>
<body>
    <%- include ('partials/navbar.ejs'); %>
   <div class="container">
     <br>
    <h1>Perguntas</h1>
    <hr>
    <a href="perguntar" class="btn btn-primary" style=" margin: -1px auto 10px auto;">Perguntar</a>
    <% perguntas.forEach(pergunta => {  %>
      <br>
      <div class="card">
          <div class="card-body">
            <a href="pergunta/<%= pergunta.id %>" style="color: black; text-decoration: none;"><h3> <%= pergunta.titulo  %> </h3>  </a>
          </div>
          <div class="card-footer">
            <a  href= "pergunta/<%= pergunta.id %>"  class="btn btn-outline-primary">Responder</a>
          </div>
      </div>

    <% }) %>
    
           
    
   </div>

</body>
<%- include ('partials/footer.ejs'); %>